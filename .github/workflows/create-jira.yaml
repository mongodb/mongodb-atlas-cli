# synced from apix-actions/.github/workflows/create-jira.yaml
name: Create Jira Ticket

on:
  pull_request:
    types: [labeled]

permissions: {}

jobs:
  create-jira:
    if: github.event.label.name == 'create_jira'
    permissions:
      contents: read
      pull-requests: write
    uses: mongodb/apix-action/.github/workflows/_create-jira.yaml@f7d2400a387dea2661651b14bbaf8c8f46c8bf66 # main
    secrets: inherit