# synced from apix-actions/.github/workflows/create-jira.yaml
name: Create Jira Ticket

on:
  pull_request:
    types: [labeled]

permissions: {}

jobs:
  create-jira:
    if: github.event.label.name == 'create_jira'
    permissions:
      contents: read
      pull-requests: write
    uses: mongodb/apix-action/.github/workflows/_create-jira.yaml@69363d9f7ea0c3b0841ce441cd4c1e1852b7fe2c # main
    secrets: inherit