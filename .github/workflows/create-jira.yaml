# synced from apix-actions/.github/workflows/create-jira.yaml
name: Create Jira Ticket

on:
  pull_request:
    types: [labeled]

permissions: {}

jobs:
  create-jira:
    if: github.event.label.name == 'create_jira'
    permissions:
      contents: read
      pull-requests: write
    uses: mongodb/apix-action/.github/workflows/_create-jira.yaml@5a8a84233f754cf95f52502d70b508792ac77159 # main
    secrets: inherit