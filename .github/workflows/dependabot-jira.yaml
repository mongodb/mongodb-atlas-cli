# synced from apix-actions/.github/workflows/dependabot-jira.yaml
name: Dependabot PR automation

on:
  pull_request:
    types: [opened]

permissions: {}

jobs:
  dependabot:
    if: github.actor == 'dependabot[bot]'
    uses: mongodb/apix-action/.github/workflows/_dependabot-jira.yaml@f7d2400a387dea2661651b14bbaf8c8f46c8bf66 # main
    secrets: inherit