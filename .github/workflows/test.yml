---
name: Test Andrea Action
on:
  push:
    branches:
      - master
  pull_request:
jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - id: foo
        uses: andreaangiolillo/atlasCLI-github-action@v0.0.4
        with:
          who-to-greet: 'Mona the Octocat'